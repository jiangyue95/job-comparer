spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  flyway:
    enabled: false

jwt:
  secret: "J26Gx*4NB!APWe0V2$r$%OzMGM13gT7$"
  expiration: 3600000

aws:
  s3:
    region: eu-west-1
    bucket: test-bucket

app:
  admin:
    email: admin@test.local
